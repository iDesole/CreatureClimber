namespace CreatureClimb
{
    /// <summary>
    /// Fixed clock options for <see cref="GameMode.TimeTrial"/>.
    /// Values equal the duration in seconds so casting never drifts from gameplay.
    /// </summary>
    public enum TimeTrialLength
    {
        Seconds90 = 90,
        Seconds120 = 120,
        Seconds200 = 200
    }
}
